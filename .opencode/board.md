# Board — Live Task Coordination

Every instance: claim before you edit, update when you finish. Rows move
Queued → Claimed → Done (or Blocked). Newest claims at top of their section.

## Active claims

| Instance | Branch | Scope (exact files/area) | Intent | Since |
|----------|--------|--------------------------|--------|-------|
| — | | | | |

## Queued (unclaimed, safe to take)

| # | Scope | Notes |
|---|-------|-------|
| 0 | docs/PJ-REDESIGN-BRAINSTORM.md | MASTER PLAN: futures-first redesign per owner Whop screenshots. Phase 1+2 DONE; remaining: Phase 3 CMS backend PR (owner-gated), Phase 4 starter-course funnel + analytics |
| 1 | owner assets | Video (≤25MB at assets/pj-intro.mp4 or YouTube URL), team bios/photos for the 5 blank slots, results-wall screenshots, $129 tier's Whop product URL if separate |

## Done

| Instance | Scope | Result | Date |
|----------|-------|--------|------|
| claude (autonomous maintenance run) | pj: tests/visual-polish.test.mjs | 252/252 tests green going in; live-deployment curl probe again unavailable (agent-proxy `status` still shows `pj.vjm.pages.dev` 403ing as a policy denial, re-confirmed via curl and WebFetch's `EGRESS_BLOCKED`, same standing condition every prior run has hit — not re-logged as new; also re-ran WebFetch against the bare domain directly and got the identical error, so it's not a `pj.` subdomain quirk). Ran a fresh accessibility/contrast/palette sweep first (symlinked the environment's global Playwright install into node_modules the way a prior run did, removed it after): tools/contrast-audit.mjs and tools/color-audit.mjs both came back clean, and a manual heading-order + internal-link/asset-reference check across all 16 HTML pages found nothing beyond the two already-tracked owner-asset placeholders (assets/results/YOUR_IMAGE.jpg, assets/team/NAME.jpg). With the frontend sweep empty, looked at what test coverage the two newest features (ebbc75a Ferrari showcase, b03730e giveaway section) actually have — the giveaway form got form-scoped newsletter coverage last run, but the Ferrari showcase (a pinned 220vh scroll band with a 3D rotateY driven by scroll progress) had zero test references of any kind. Added two tests to tests/visual-polish.test.mjs: one pinning that both bail-out paths (the phone breakpoint and prefers-reduced-motion) actually flatten the track and freeze the card transform in CSS, that backface-visibility:hidden survives as the mirrored-card guard, and that the spin script's own bail-out condition (`reduced || !track || !card || innerWidth<=760`) is present verbatim; the other decoding assets/pj-ferrari.jpg's real JPEG SOF0 dimensions and asserting they match the markup's declared width/height (the same class of bug as the earlier testimonials.webp CLS incident), plus that the alt text describes the photo rather than naming the file. Verified both catch real regressions: temporarily collapsed the reduced-motion CSS block to just `.fs-track{height:auto}` and watched the first test fail, then mismatched the declared width (482→500) and watched the second fail; both reverted before restoring. No bug found — the shipped Ferrari showcase was already correct on every axis checked. 254/254 tests; commit pushed to origin/pj. | 2026-09-02 |
| claude (autonomous maintenance run) | pj: tests/newsletter-api.test.mjs | 251/251 tests green going in; live-deployment curl probe again unavailable (agent-proxy `status` still shows `pj.vjm.pages.dev` 403ing as a policy denial, re-confirmed via curl and WebFetch's `EGRESS_BLOCKED`, same standing condition every prior run has hit — not re-logged as new). With coverage already at 99.02%/86.07%/92.88% line/branch/func repo-wide, swept the two features landed earlier today (ebbc75a Ferrari showcase, b03730e giveaway section) instead — new features are the likeliest place for an unswept gap. Found one: `tests/newsletter-api.test.mjs`'s "every signup form carries consent + opt-out" test asserts against the whole page as one string, so once index.html grew a second `.nl-signup` form (the giveaway section, alongside the pre-existing "home" one) it could no longer tell whether the NEW form specifically had its own honeypot/consent/live-region — it would keep passing as long as the other form on the page still had them, i.e. it would not have caught the giveaway form shipping broken. Added a form-scoped test that walks each individual `<form class="nl-signup">` on index.html/prop-firms.html and checks data-source, required email, honeypot, unticked consent, an aria-live status region, and a consent-checkbox id unique across the page. Verified it actually catches the gap: stripped the giveaway form's honeypot alone, watched the new test fail while everything else stayed green, restored. No bug found in the shipped markup — both new sections were already correctly wired (backend `source` field is free-text already, no enum to extend). 252/252 tests; commit pushed to origin/pj. | 2026-09-02 |
| claude (autonomous maintenance run) | pj: tests/backtest-core-lib.test.mjs | 251/251 tests green going in; live-deployment curl probe again unavailable (agent-proxy `status` still shows the CONNECT to pj.vjm.pages.dev 403ing as a policy denial, re-confirmed via curl and WebFetch's `EGRESS_BLOCKED`, same standing condition every prior run has hit — not re-logged as new). A coverage sweep found backtest-core.js (the not-yet-wired Edge Lab engine) the clear outlier: 88.85%/75.76%/51.61% line/branch/func, while every other functions/api/_lib/*.js file sat at 90%+ on all three. Its own existing test file only ever called two of the 11 TRIGGERS definitions (gap_up, streak_reversal) directly, leaving gap_down/inside_day/outside_day/nr7/sma_break/vol_spike/rsi_cross/new_n_day_close_high/new_n_day_close_low/big_move's `.test()` logic — the actual signal-detection math the UI's trigger picker will call — completely unexercised, along with triggerWarmup's period/n-resolution branches, simulateEvent's ordinary single-sided stop-only/target-only exits (only the timeout and same-bar-ambiguous cases had coverage), runEventStudy's tail-of-series dropped-incomplete counting and its risk-adjusted stats block (targetHitRate/stopRate/avgRMultiple/medianRMultiple, only populated when stopR/targetR are configured), and monteCarloPaths' ruin counter. Added direct-call coverage for all of it, each case checked against a hand-picked bar where the trigger must fire and a minimally-different one where it must not (plus the sma_break/big_move null-indicator warmup guards). No bug found on inspection — all 11 trigger definitions are correct as written — but two engineered scenarios were non-trivial to get exactly right the first time: the risk-stats test needed targetR===stopR so the resulting R-multiples come out at exactly ±1 regardless of the tape's actual ATR value, and the ruin-counter test needed enough compounding steps (100×0.95^50≈7.7) to actually cross the 25-equity ruin line, not just approach it. backtest-core.js coverage rose to 100.00%/87.03%/82.26% line/branch/func; whole-repo coverage 98.65%→99.04% line, 85.58%→86.08% branch, 91.48%→92.88% func. 251/251 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: assets/newsletter.js; tests/newsletter-api.test.mjs | 227/227 tests green going in; live-deployment curl probe again unavailable (agent-proxy `status` confirms the same standing `connect_rejected` policy denial on pj.vjm.pages.dev, re-confirmed via curl and WebFetch's `EGRESS_BLOCKED`, not re-logged as new). `tools/contrast-audit.mjs` and `tools/color-audit.mjs` (added last run) both ran clean against the newsletter/prop-firm-playbook/schedule/billing-tab changes landed since the last full sweep — no contrast or palette regressions (playwright isn't a repo dependency; ran it via a local `node_modules/playwright` symlink to the environment's global install, removed after, nothing committed). Reading the new newsletter feature's one-click unsubscribe flow found a real copy bug instead: the client-side 'invalid' state message claimed a link "is not valid or has already been used", but `onRequestGet` in functions/api/newsletter/unsubscribe.js never actually reaches 'invalid' for a reused token — a well-formed token that matches zero rows (already unsubscribed, or never issued) still redirects to 'done', same as the POST form's own "never report whether a row matched" rule; 'invalid' only fires for a malformed/missing token param. So the copy described a failure mode a re-clicked link can never actually produce. Fixed the wording to just "is not valid", and added a test proving the reused-token case redirects to state=done (not invalid) plus a regression test on the copy string itself. Confirmed the new copy test fails against the old wording before restoring the fix. 228/228 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: functions/api/research-engine.js; tests/research-engine.test.mjs | 201/201 tests green going in; live-deployment curl probe again unavailable (agent-proxy `status` still shows the CONNECT to pj.vjm.pages.dev 403ing as a policy denial, re-confirmed via curl and WebFetch's `EGRESS_BLOCKED`, same standing condition already flagged for the owner — not re-logged as new). Also swept all 15 shipped HTML pages via a dedicated agent for copy/alt-text/label/heading-order defects (the class of issue this run's priority order puts ahead of test coverage): came back clean — every image, form control and heading level already correct, the two owner-asset placeholders are the only known gaps and already tracked. With the frontend sweep empty, fell back to a coverage sweep (`node --test --experimental-test-coverage`), which found `swingLiquidityLevels` — the pivot-high/pivot-low detector that backs the "Overnight BSL/SSL" liquidity-sweep conditions research-engine.js scans for and the "Latest Overnight BSL/SSL" rows shown in the premium dashboard's Latest Levels panel — was the last untested helper in the file's SMT/intraday-event surface (line 557, 0% direct coverage; buildIntradayEvents/buildSmtEvents/buildLatestLevels, which call it, remain orchestration-only gaps at lines 387-397/432-452/522-526, not picked up this run). Added it to `__test` and pinned: a genuine interior pivot high/low against non-pivot neighbors on both sides, the boundary exclusion (a bar within `pivot` positions of either array edge can never register, even if it holds the array's true extreme — the window can't be fully checked), a null high/low inside the pivot window not breaking the comparison, and the `_source` passthrough vs. its 'Observed' fallback. No bug found on inspection; confirmed the source-fallback assertion fails when the `||'Observed'` default is dropped, then reverted. Coverage 93.88%→94.05% line, 74.29%→74.65% branch, 91.18%→91.86% func. 201/201 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: tests/research-api.test.mjs | 201/201 tests green going in; live-deployment curl probe again unavailable — this run confirmed the block is total, not `pj.vjm.pages.dev`-specific or even `*.pages.dev`-specific: `example.com` and `cloudflare.com` CONNECT attempts also 403 via the agent-proxy (re-confirmed via `status`, WebFetch's `EGRESS_BLOCKED`, and raw curl), the same standing condition every prior run has hit and already flagged for the owner once (2026-08-31 entry). Coverage sweep (`node --test --experimental-test-coverage`) found research-engine.js the weakest file overall: 84.46%/73.87%/88.61% line/branch/func. tests/research-api.test.mjs, the handler-level test file, had only ever driven module=health/options/intraday through onRequestGet — module=stock, module=sectors, and module=biotech (three of the six research modules the premium dashboard calls) had zero handler-level coverage. Also untested: the unknown-module 400, the missing-Alpaca-config 503, and — the more consequential gap — the D1 cached-snapshot fallback (saveSnapshot/loadLatest) that's supposed to keep the Research Engine serving data instead of an error page during an Alpaca outage; nothing in the whole suite had ever exercised that round trip. Added coverage for all three modules (sectors' relative-strength sort verified to actually rank the outperformer first via a temporarily-broken-sort check, not just assert unsorted rows), the two shared error paths, and the full D1 fallback (a live fetch succeeds and saves, an identical second request has Alpaca fail and must serve the exact saved snapshot back with cached:true, not a hard error) using a fake D1 matching market-brief.js's existing research_latest fixture shape. No bug found on inspection. Coverage rose to 93.88%/74.29%/91.18% line/branch/func. 201/201 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: tests/verify-premium-api.test.mjs | 200/200 tests green going in; live-deployment curl probe again unavailable (this sandbox's egress gateway 403s the CONNECT to pj.vjm.pages.dev, re-confirmed via agent-proxy status, same standing network-policy block every prior run has hit — not a new finding). Coverage sweep (`node --test --experimental-test-coverage`) found verify-premium.js at 87.65%/75.34%/80.00% line/branch/func, the lowest of any auth-critical handler, with a whole exported code path at zero coverage: `lookupViaSecureBridge`, the preferred MEMBERS_BRIDGE_URL/MEMBERS_BRIDGE_SECRET authenticated single-record bridge. Today's earlier member-status-api.test.mjs entry had already named this exact gap in its own comment (bridgeLookup mirrors "verify-premium.js's own lookupViaSecureBridge") without closing it. Added coverage for: a success signing the documented `${timestamp}\n${nonce}\n${bodyJson}` HMAC-SHA256 request (asserted against an independently-recomputed verify(), including that it fails under a different secret), that the secure bridge is used instead of MEMBERS_STATUS_URL when both are configured, 'renewed' counting as live same as 'active', not-found and non-live-status both landing on the generic bad-code message, and the bridge being unreachable/non-ok/returning malformed JSON. Confirmed the 'renewed' branch assertion fails (401 instead of 200) when narrowed to `status !== 'active'` alone, then reverted. Coverage 87.65%→97.65% line, 75.34%→81.72% branch, 80.00%→94.12% func. 200/200 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: functions/api/market-brief.js, tests/market-brief-api.test.mjs | 192/192 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain — this run also confirmed it isn't Cloudflare-specific by hitting `example.com` directly via WebFetch and getting the identical `EGRESS_BLOCKED` denial, not a new finding). Coverage sweep found market-brief.js the weakest handler left: 75.91%/66.04%/52.38% line/branch/func — its existing test file only exercised the rate limit, pending/cached GET, the POST auth gate, and the all-third-party-fails degrade path, never a single success. Along the way found genuine dead code: `pick()` and `decodeEntities()`, XML/RSS helpers left behind when the file's headline fetch migrated to Yahoo's JSON search endpoint (the migrating comment is still in the code) — zero call sites, deleted, and the stale "via public Yahoo RSS" section comment corrected. Added coverage for Alpaca proxies+movers succeeding, Yahoo headlines succeeding, the ForexFactory calendar count succeeding, and maybePostToDiscord's no-narrative branch (formats brief.lean/brief.movers itself) plus its "webhook rejected" detail string, none of which any test had reached before. Coverage 75.91%→98.97% line, 66.04%→76.29% branch, 52.38%→81.82% func (remaining: the outermost 502 catch-all in onRequestPost, and parseUniverse's BRIEF_UNIVERSE override path, neither exercised by any real request the site sends today). 192/192 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: tests/forex-calendar-api.test.mjs | 192/192 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status — this run also confirmed the block isn't `pj.vjm.pages.dev`-specific by hitting `example.com` and `cloudflare.com` directly and getting the identical `connect_rejected` policy denial, not a new finding). Coverage sweep (`node --test --experimental-test-coverage`) found forex-calendar.js at 85.50%/44.74% line/branch — the lowest branch coverage of any handler — with only its impact-filter happy path tested (tests/forex-calendar-api.test.mjs, last touched 2026-08-2x per git log). Untested: the 30/min rate-limit guard, the 400 an out-of-set impact value gets, the unsupported-currency-falls-back-to-USD guard, currency=ALL widening past USD, and both upstream-failure branches the file's own header comment promises — fail-closed 502 with no cached copy, and re-serve the last good copy (labeled stale, with its original fetchedAt) when faireconomy throttles Cloudflare's shared egress IPs. Added all six as regression cases, proving the rate-limit guard blocks the 31st request before the upstream feed is ever called again (asserted by counting real fetch invocations, not just status codes). No bug found on inspection; sensitivity-checked the stale-fetchedAt assertion by reverting it to always report `now()` and watching it fail, then restored. Coverage 85.50%→99.24% line, 44.74%→72.55% branch (the one remaining gap, line 123, is `impactClass`'s 'low' branch, which the major/high/medium filter set never reaches). 192/192 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: tests/member-status-api.test.mjs | 192/192 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status, not a new finding). Every functions/api/*.js and _lib/*.js file already had *some* direct test coverage, so this run used `node --test --experimental-test-coverage` for the first time instead of inspection-based gap-hunting, and it found a function-level gap real audits had missed: check-member-status.js was at 75.54% line coverage because its MEMBERS_BRIDGE_URL/MEMBERS_BRIDGE_SECRET authenticated-bridge path (`bridgeLookup`, an HMAC-SHA256 signed lookup mirroring verify-premium.js's own `lookupViaSecureBridge`) had zero references — the existing test only ever configured the legacy MEMBERS_STATUS_URL path. Added the 8/min rate-limit-before-configured-check case and the full bridge contract (request shape, payload matching verify-premium's own {type,value} shape, an independently-recomputed HMAC verify() confirming the mac authenticates under the right secret and not a wrong one, found-but-inactive/not-found/ok:false all collapsing to the same generic 404, non-ok/thrown fetch failing closed to 502). Verified the mac assertion fails when the signed message's field order is swapped, then reverted. Coverage 75.54%→98.56%. 192/192 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (autonomous maintenance run) | pj: tests/research-engine.test.mjs | 192/192 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status, not a new finding). Every functions/api/*.js and _lib/*.js file, plus 27 of research-engine.js's 28 `__test`-exported helpers, already had direct coverage; `buildPriceActionModel` — the orchestrator that scans all four FVG/IFVG timeframes (1/5/15/60m) plus displacement across the trailing N trading days and feeds the premium research dashboard's fvgStats/conditions/timing — was the one exception. Added coverage pinning: a day with under 30 RTH bars is skipped entirely, `days` (limitDays) selects the most recent N calendar dates by sort rather than an arbitrary N map entries, and the combined output is the exact ordered union of scanning every timeframe plus the displacement scan on each included day (a strict deepEqual against calling those already-tested helpers directly). Confirmed the union assertion fails when the 60m timeframe is dropped from the loop, then reverted. 192/192 tests; commit pushed to origin/pj. | 2026-09-01 |
| claude (owner-directed, Claude Code on the web) | pj: funnel event layer, tier-aware upgrade moment, free-progress save, paywall measurement — PR #5 branch | The audit's section 3 lever. Site had zero on-site analytics (no gtag/dataLayer/plausible anywhere), the homepage trader quiz dead-ended at Retake instead of routing to the track its own answers imply, free-lesson completion saved nothing and prompted nothing, and course locks showed an access-code box to signed-in under-tier members — the highest-intent moment on the site. Adds a sink-agnostic event layer (no vendor chosen: owner's call, and the CSP would block one), quiz routing, lead capture wired to a marked stub rather than a fabricated backend, per-plan CTAs, tier-aware lock/upgrade state exposing the $29 Core->Complete step, and local free-progress save. Also tools/paywall-audit.mjs + docs/PAYWALL.md quantifying the one unresolved structural problem — all paid lesson bodies readable in public source — as a costed decision rather than a repeated warning. | 2026-08-31 |
| claude (owner-directed, Claude Code on the web) | pj: worked-case arithmetic audit, research look-ahead, entitlement authority, lesson companion, homepage truth, SEO consolidation — PR #5 | Two findings outrank the tasks that surfaced them. (1) Look-ahead bias in the research studies shown to paying members: four defects, one the outside audit missed (displacement scanning). Resampled bars were stamped at group OPEN while carrying group-COMPLETE OHLC, giving a 60m FVG up to 59 minutes of foresight; on a seeded random walk 60m FVG retest fell 100->50%, 15m 100->66.7%, continuation 100->33.3%. Every displayed rate was overstated, one-directionally, and cached D1 snapshots still serve pre-fix numbers — someone must decide on invalidation and on telling members. (2) 17 of ~990 recomputed figures in the twelve worked cases were wrong, incl. a 73-vs-72-day carry error attributing a whole basis move to dividends, and a drawdown baseline whose stated mu contradicted its own win/loss stats and fed every threshold in its table. Also: a canceled member kept every paid page for up to 30 days (stateless cookie, three sources of truth) — D1 is now authoritative with a per-member session epoch, and two paid endpoints were bypassing even the new check. Lesson companion now answers from server-held text with tier enforcement and cite-or-refuse. SEO consolidated to one origin and one indexing switch; CNAME deliberately untouched (live DNS is the owner's call) and still disagrees. 149/149 tests. | 2026-08-31 |
| claude (owner-directed, Claude Code on the web) | pj: entitlements, premium API auth, quiz integrity, twelve worked cases, claims/disclosures — PR #4, merged | The two paid products were one product: verify-premium.js signed {v,mr,dn,exp} with no tier and _middleware.js gated all four courses on `!!getSession()`, so a $100 Futures buyer received the $129 Complete library and the Core->Complete upgrade was mathematically impossible to sell. Added functions/api/_lib/entitlements.js: signed tier claim, one server-side resource->tier table, an unrecognized Whop product grants nothing, unknown tier or requirement fails closed. The Research Engine accepted only a bearer token the browser never sends, so the UI said 'unlocked' and every real request 401'd. Quizzes scored 96/100 by clicking slot 1 (96 of 100 answers at index 1); choices now shuffle at render keyed to original index and the audit tool fails CI if the shuffle is removed. Claims, risk disclosure, terms, and a privacy statement that was flatly false about stored Whop data all corrected. NOTE: strict tier mode stays OFF until WHOP_PRODUCTS_FUTURES/COMPLETE are set — that config, not the merge, is what actually separates the products. | 2026-08-31 |
| claude (owner-directed, Claude Code on the web) | pj: neutral palette rebrand across all shipped HTML/CSS/JS — PR #3, merged | The gold/emerald/blue palette had been mass find-replaced to red, so red was the design system rather than an accent: borders, glows, headings, 'positive' states and P&L up-ticks were all the same alarm red. Remapped every colour literal to a neutral white/black/grey scale with red as a restrained accent, took red off display type, and gave every page a working light theme (only two pages previously read back the stored preference; assets/theme.js now applies it site-wide). Red fell 40.9->19.4% of colour literals and stray non-red hues 156->0; measured red pixel coverage across 15 pages x 2 themes fell 3.47->1.06% average, worst page 12.04->2.74%. tools/color-audit.mjs + tests/palette.test.mjs prevent regression. | 2026-08-31 |

| claude (autonomous maintenance run) | pj: stock-lab.html; tests/site-contract.test.mjs; .opencode/board.md | 133/133 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status, not a new finding — already flagged for the owner in a prior run's entry). Today's own entitlements work (commit eb6bbb9/2f82e36) gave research-engine.html a dedicated "your plan does not include this" state for a 403 upgrade_required response from the now Complete-tier-gated premium APIs, but stock-lab.html's premium gate only checks "is there any active session" and its `runPremiumResearch()` (calling the same now-tiered /api/premium-stock-research) still fell through to the generic catch, which appends "Unlock again or verify the source links manually." — actively wrong advice for a Futures Core member whose code is valid and active, since re-entering it can never grant a Complete-only tool. Fixed by tagging the thrown error with `planLocked` (403 or `code==='upgrade_required'`) and branching the copy, same pattern research-engine.js already established. Added a regression test to tests/site-contract.test.mjs pinning both the detection and that the plan-locked branch never renders the re-unlock copy; confirmed it fails against the pre-fix markup (stashed stock-lab.html, watched it fail, restored) before landing the fix. Also added a board.md "Blocked on owner" row for WHOP_PRODUCTS_FUTURES/WHOP_PRODUCTS_COMPLETE — until the owner sets those, resolveTier() grants everyone `complete` and the two paid tiers stay functionally identical, per docs/ENTITLEMENTS.md's own note that this is the step that actually fixes the revenue bug. 134/134 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: index.html; tests/regressions.test.mjs | 102/102 tests green going in; live-deployment curl probe again unavailable — this sandbox's network egress policy denies all CONNECT attempts to pj.vjm.pages.dev and the whole *.pages.dev domain (confirmed via curl, agent-proxy `status` (`connect_rejected`, policy denial), and WebFetch), a standing condition every run this session has hit, so pushed a proactive notification recommending the environment's egress allowlist be updated since this makes step 2 permanently unverifiable here otherwise. With every functions/*.js file and its known edge cases already under test (see prior entries) and research-engine.js's __test surface closed out last run, swept HTML pages for a fresh angle instead of redoing that ground: checked _headers' /assets/* Cache-Control (currently max-age=300, must-revalidate) against git history first and found it was deliberately lowered from immutable/86400s by the owner's own commit a644036 after a real "fixes not applying" incident (assets have no content-hashed filenames) — correctly left alone, not touched. Found a real gap instead: 3 of the site's ~90 target="_blank" links (all on index.html — the members-dashboard "Join Discord"/"DM Support" cards, and the psychology-essay footer Discord CTA) had no rel="noopener", unlike every other external link on the site, which is a reverse-tabnabbing gap (the opened Discord tab keeps a window.opener handle back to a signed-in member's tab). Added rel="noopener" to all three and a regression test asserting every target="_blank" anchor sitewide carries it (checks raw source, not script-stripped, since some anchors are JS template strings). Confirmed the test fails on the pre-fix markup (stashed index.html, watched it fail, restored). 103/103 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/research-engine.test.mjs | 102/102 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status and WebFetch, not a new finding). Every functions/api/*.js and functions/api/_lib/*.js file now has direct test coverage, so this run audited research-engine.js's own `__test` export surface (18 internal helpers deliberately exposed for direct testing) and found 4 of them — `median`, `statsFromFibEvents`, `combineFibStats`, `summarizeFvg` — had zero direct test references despite backing real numbers the premium research dashboard shows members (fib-retracement fill rates/median MFE-MAE, FVG retest/continuation rates). Added assertions to tests/research-engine.test.mjs covering median's empty/single/even-length/non-finite-input branches, statsFromFibEvents always returning all three tracked fib levels (including a zero-touch level reporting null rates, not 0), combineFibStats actually pooling events across multiple timeframe results rather than only reading the first, and summarizeFvg's timeframe+side grouping key plus its median-ignores-null-minutesToRetest behavior. No bug found on inspection (all four are straightforward and correct), but spot-checked test sensitivity by breaking median's even-length averaging and confirming the suite fails, then reverted. 102/102 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/turnstile-lib.test.mjs | 96/96 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status and WebFetch, not a new finding). turnstile.js was the last functions/api/_lib/*.js file with zero direct test references — only ever exercised transitively through verify-premium-api.test.mjs's mocked fetch. It's also the bot-protection check the run cycle's own curl probe targets and can never actually reach in this sandbox, so a direct unit test is the closest available substitute. Added tests/turnstile-lib.test.mjs pinning the no-token short-circuit (never calls fetch), a non-ok Cloudflare response and a thrown network error both failing closed, success requiring the strict `data.success === true` boolean (a truthy non-boolean like the string "true" must still fail), the documented siteverify request shape, and remoteip being omitted (not sent empty) when no ip is available. Confirmed the strict-boolean assertion fails when the check is loosened to `data.success`, then reverted. 102/102 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: index.html; tests/regressions.test.mjs | 95/95 tests green going in; live-deployment curl probe again unavailable (agent-proxy `status` shows a `connect_rejected` policy denial on pj.vjm.pages.dev, confirmed via both curl and WebFetch, not a new finding). With every functions/*.js file and its known edge cases already under test, swept HTML pages for accessibility/performance gaps instead. Found index.html's testimonials collage (`<img src="assets/testimonials.webp" loading="lazy" style="width:100%;height:auto;...">`, no width/height attributes) was the one static lazy image on the site without intrinsic dimensions — every sibling lazy image (video poster) already sets them. No reserved aspect-ratio box means the browser collapses it to 0 height until the ~2000x1125 webp loads, so the page jumps right above the "Already a Member?" CTA when a visitor scrolls to it — real CLS, not a nitpick. Decoded the webp's own VP8X header to confirm actual pixel dimensions (2000x1125) rather than guessing, added `width="2000" height="1125"`, and added a regression test in tests/regressions.test.mjs asserting every literal (non-CMS-generated) `loading="lazy"` `<img>` across all HTML pages carries both attributes — strips `<script>` blocks and HTML comments first so it doesn't false-positive on the two CMS-driven `<img>` tags built from owner-uploaded photos of unknown size, or the commented-out results-wall template. Confirmed the test fails on the pre-fix markup (stashed index.html, watched it fail, restored). 96/96 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/security-auth.test.mjs | 94/94 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status, not a new finding). Every functions/api/*.js and functions/api/_lib/*.js file now has direct test coverage (alpaca.js closed the last gap last run), so this run audited each tested file's exports for untested edge cases instead of untested files. sessionDays() in _lib/session.js — the function that turns the owner-configurable SESSION_DAYS env var into the session cookie's Max-Age, with a documented 30-day hard cap so a yearly Whop plan (or a fat-fingered env value) can never mint a long-lived token — had every call site tested only through its default (unset) branch; the cap itself was never exercised. Added a test to tests/security-auth.test.mjs (the file that already owns session.js's other security invariants) pinning the default-when-unset/zero/negative/non-numeric cases, the round-up-not-down rounding, and the cap holding at exactly 30, one over, and wildly over (365). Confirmed the cap assertion fails (31 instead of 30) when the Math.min(..., MAX_SESSION_DAYS) is dropped to a bare Math.ceil, then reverted. Full sweep of HTML pages/copy found nothing new beyond the two already-tracked owner-asset placeholders. 95/95 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: functions/api/_lib/alpaca.js; tests/alpaca-lib.test.mjs | 93/93 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status, not a new finding). alpaca.js was the last functions/api/_lib/*.js file with zero direct test references (its five importing handlers all mock fetch at the handler level, never exercising this file's exports directly). Writing computedMovers' coverage found a real bug: with the site's actual fixed universes (assistant.js's UNIVERSE is 8 symbols, market-brief.js's DEFAULT_UNIVERSE is 10), an unconditional rows.slice(0,8)/rows.slice(-8) meant that whenever 8 or fewer symbols had valid data, gainers and losers came back as the exact same set, just reordered — a flat day would show every symbol as both a "top gainer" and a "top loser" in the AI assistant's and market brief's fallback-movers path (triggered whenever Alpaca's official screener endpoint is down/rate-limited). Fixed by filtering rows by sign before slicing. Added tests/alpaca-lib.test.mjs covering alpacaConfigured, snapshots' request shape and top-level (unwrapped) response reading, summarizeSnapshot's null/NaN/zero-prevClose guards, movers' cap-at-8 and fail-soft-to-null, and computedMovers' sort/filter/slice including an explicit 8-symbol overlap regression case. Confirmed the overlap assertion fails on the pre-fix slice logic (stashed the fix, watched it fail, restored it). 94/94 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/discord-lib.test.mjs | 93/93 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on the whole *.pages.dev domain, re-confirmed via agent-proxy status, not a new finding). Full sweep found nothing else broken. discord.js was now the last functions/api/_lib/*.js file with zero test references. It's imported by whop-webhook.js, market-brief.js, and content-sync.js, but every one of those handlers' tests only mocks globalThis.fetch to return a status code, never inspecting the request body postEmbed sends — so the safety rails the file's own header comment promises (@everyone/@here de-fanging, embed size limits, mentions disabled by default) had never been exercised even indirectly, despite the embeds carrying CMS/announcement text that isn't fully owner-controlled. Added tests/discord-lib.test.mjs covering sanitizeDiscordText's mention-stripping, control-char removal, coercion, and 3900-char truncation; postEmbed's host+path allowlist rejecting before any fetch call, the sanitized/truncated/25-field-capped payload actually sent, the allowMentions override, and the 204/429/thrown-error outcome mapping. Confirmed the mention-stripping assertion fails when the @everyone/@here replace is removed, then reverted. 93/93 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/ai-lib.test.mjs | 92/92 tests green going in; live-deployment curl probe again unavailable — same standing network-policy 403 on pj.vjm.pages.dev, and this run also confirmed the block covers the whole *.pages.dev domain (tried the bare vjm-site.pages.dev host too, same 403), not just the pj subdomain; re-confirmed via agent-proxy status, not a new finding. Full sweep found nothing else broken. ai.js — the Workers-AI wrapper functions/api/assistant.js, market-brief.js, and premium-market-analyst.js all import — was the last functions/api/_lib/*.js file with zero test references (discord.js remains open). None of those three handlers' tests ever set env.AI, so every one of them only exercises aiConfigured() returning false; complete()'s actual request shape and its result.response/result.result/result.text fallback chain (the same kind of defensive unwrap that was once genuinely wrong for Alpaca's snapshot shape elsewhere in this codebase) had zero coverage, direct or indirect. Added tests/ai-lib.test.mjs covering aiConfigured's function-type check, the request shape (system message prepended only when given, default vs overridden model/maxTokens), the three-way result-shape fallback and its priority order, whitespace-only/non-string/null results collapsing to null, the AI binding throwing degrading to null instead of propagating, and MARKET_GUARDRAILS still carrying its no-personalized-advice and educational-only lines. Confirmed the fallback-chain assertion fails when the result.result/result.text fallback is removed, and the fail-soft assertion fails (uncaught throw) when the catch block is changed to rethrow; both reverted. 92/92 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/integrations-core-lib.test.mjs; functions/api/_lib/integrations-core.js | 90/90 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. integrations-core.js — its own comment calls itself "pure helpers... unit-testable deterministically" — was the last _lib/*.js file imported by tested handlers (content-sync.js, whop-webhook.js, market-brief.js) with zero direct test imports (ai.js and discord.js remain in that state). Added tests/integrations-core-lib.test.mjs covering timingSafeHexEqual's length/empty-string edge cases, normalizeWhopEvent's grant/revoke/ignore classification and field enrichment, generateAccessCodeShape/isValidGeneratedCode's round trip, computeFuturesLean's exact-boundary thresholds and QQQ double-weighting, and sanitizeContentRow across all 9 content types — 8 of which (all but announcements) had never had their required-field validation exercised even indirectly, since content-sync-api.test.mjs only posts announcements rows; also pinned cleanUrl's https-only allowlist and cleanStr's @everyone/@here zero-width-space guard. Writing the round-trip test found a real (low-impact) bug: isValidGeneratedCode's regex used a J-N range that let L back in even though the generator's own alphabet comment says "no I/L/O/0/1 confusion" — harmless today only because the generator never emits L, so fixed the regex to enumerate J,K,M,N explicitly. Confirmed the L-exclusion assertion fails on the pre-fix regex, then applied the fix. 91/91 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/http-lib.test.mjs | 89/89 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. Every functions/api/*.js handler, _middleware.js, the video Range handler, and _lib/backtest-core.js already had direct coverage; of the remaining _lib/*.js files with zero direct test imports (ai.js, alpaca.js is covered indirectly, discord.js, http.js, integrations-core.js), http.js stood out: it's the shared rate-limit/response/symbol-validation module every handler imports, and the exact file the run cycle's own hard rules name ("never remove rate limiting"). Every existing handler test only exercises checkRateLimit()'s in-isolate Map fallback (no test env sets RATELIMIT_DB) — the D1-persisted path that actually holds the limit across isolates in production had zero coverage of its own: the INSERT..ON CONFLICT increment, the count read-back, the count===1 stale-bucket sweep, and the "D1 throws -> fall back to memory, never fail open" catch. Added tests/http-lib.test.mjs covering all of that plus cleanSymbol's exchange-prefix stripping/character allowlist, clientIp's header/fallback/truncation, and the no-store/nosniff headers on every JSON reply. Confirmed the sweep assertion fails (3 deletes instead of 1) when the count===1 guard is widened to always-sweep, and the D1-path assertion fails (allowed:true past the limit) when the return is hardcoded to allowed:true; both reverted. 90/90 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: tests/backtest-core-lib.test.mjs | 88/88 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. Every functions/api/*.js file, _middleware.js, and functions/video/pj-intro.mp4.js already had direct handler coverage (prior runs), so this run looked at functions/api/_lib/*.js and found backtest-core.js — a 536-line deterministic event-study/backtest engine (sma/atr/rsi indicators, 9 triggers, trade simulation, Monte Carlo) — with zero test references; only its wilsonInterval export is currently re-used elsewhere (integrations-core.js), so the module is latent rather than dead, per commit 83dc371's own note that it's "imported only for wilsonInterval" today, ahead of being wired up as a paid Edge Lab feature. That same commit fixed two real bugs here (short-side MFE/MAE excursions read the wrong extreme, and streak_reversal tested the reversal day with the same sign as the streak so it never actually detected a reversal) and nothing had pinned either fix. Added tests/backtest-core-lib.test.mjs covering: simulateEvent's short vs long MFE/MAE symmetry (a short's favourable excursion must read the low, its adverse excursion the high — exact mirror of a long on the same bars), the same-bar stop+target ambiguity resolving pessimistic stop-first, streak_reversal firing on a true reversal but not on a same-direction continuation day (both directions), causal sma/atr/rsi sanity, wilsonInterval's zero-sample edge case and interval-narrows-with-sample-size property, summarizeReturns' empty/small-sample warnings, monteCarloPaths' seeded determinism, and an end-to-end runEventStudy smoke test. Confirmed the MFE/MAE assertions fail (mfe 205 instead of 2, mae 198 instead of -5) when the short-side extremes are swapped back to the pre-fix formula, then reverted. 89/89 tests; commit pushed to origin/pj. | 2026-08-31 |
| claude (autonomous maintenance run) | pj: functions/video/pj-intro.mp4.js; tests/video-api.test.mjs | 87/87 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). With every functions/api/*.js file and _middleware.js already covered (prior run), swept the rest of functions/ and found functions/video/pj-intro.mp4.js — the KV-backed Range-request handler the intro video's <video> tag depends on — had only a textual wiring check (tests/pj-futures.test.mjs confirms index.html points at /video/pj-intro.mp4), never a call into onRequestGet/onRequestHead. Writing that coverage surfaced a real bug, not just a gap: a suffix byte-range ("bytes=-500", RFC 7233 §2.1 — "the last 500 bytes") was parsed identically to a leading-empty range, giving start=0 and returning the *first* 501 bytes instead of the last 500. Some MP4 players issue exactly this request to fetch a trailing moov atom or to scrub to the end of the file, and would have silently gotten the wrong slice back as a "successful" 206 — no error, just corrupt playback. Fixed by special-casing match[1] === '' to compute start = total - suffixLength before falling through to the shared 416/clamp logic. Added tests/video-api.test.mjs covering the no-KV-binding 503, not-yet-uploaded-object 404, full-file 200 with no Range header, HEAD's size/Accept-Ranges without a body, a normal start-end range, an open-ended start- range, a range clamped to EOF, the suffix-range fix itself (bytes=-500 must equal bytes 500-999, not 0-500), a suffix range longer than the whole file clamping to byte 0, and malformed/unsatisfiable ranges 416ing instead of serving wrong data. Confirmed the suffix-range assertion fails (Content-Range: bytes 0-500/1000 instead of 500-999/1000) on the pre-fix code, then re-applied the fix. 88/88 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/middleware.test.mjs | 86/86 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. Every functions/api/*.js file now has direct handler test coverage (market-brief.js closed the last gap last run), so this run looked past api/ and found functions/_middleware.js — the actual server-side enforcement point for the site's core "only two lessons are free" invariant — had zero behavioral coverage, only a textual/regex check (tests/regressions.test.mjs) that the source *mentions* getSession/HTMLRewriter/.gated-content. Node has no global HTMLRewriter (Workers-only API), which is presumably why no one had exercised onRequest() directly before; added a minimal class-selector HTMLRewriter test double (matches by class token, tracks nesting depth by tag name, scoped to exactly what this middleware uses) so tests/middleware.test.mjs could call onRequest() for real. Covers: unauthenticated GET to a gated page never leaks the paid markup into the response body while sibling free content is untouched, a valid session gets the full lesson back, a tampered cookie fails closed to the stripped path, both branches carry the same private/no-store Cache-Control and drop the stale content-length/etag, non-GET and non-gated-path requests are pure identity passthrough, and a non-2xx or non-HTML origin response is also passthrough. Confirmed the core assertion fails (paid text leaks) when `authorized` is forced to true after the getSession check, then reverted. 87/87 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/market-brief-api.test.mjs | 85/85 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. market-brief.js was the last functions/api/*.js file with only a textual wiring check (tests/pj-futures.test.mjs), no direct handler test import — assistant.js was its sibling in that state and got covered last run. Added tests/market-brief-api.test.mjs covering: the GET rate-limit gate (30/min), the "no brief yet is a 200 pending state, never a 404" contract the handler's own comment insists on, the POST auth gate (X-Research-Cron constant-time compare, unconfigured secret fails closed), the Alpaca-unconfigured and third-party-fetch-failure degrade-to-warnings paths (never a 502 just because a third party is down), the D1 cache_key/payload round-trip storeBrief/loadBrief both key on, the Discord dry-run-vs-delivered detail strings, and the file's own documented per-isolate memory fallback tying a POST's generated brief to a same-isolate GET when RESEARCH_DB is absent. Confirmed that last assertion fails when the memory-fallback read in loadBrief is disabled, then reverted. 86/86 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/assistant-api.test.mjs | 84/84 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. assistant.js and market-brief.js were the last two functions/api/*.js files with only textual wiring checks (tests/visual-polish.test.mjs), no direct handler test imports; picked assistant.js for its documented security fix: lesson-companion mode is UI-gated as members-only but the endpoint itself never checked, so anyone could POST arbitrary lessonText and use the site's Workers AI binding as a free unauthenticated LLM proxy. Added tests/assistant-api.test.mjs covering: the 8/min rate-limit gate blocking before any Alpaca/AI call, missing-question 400 before any network call, the lesson-mode session gate (missing and forged cookies both 401 before the AI binding is ever touched), a valid session with no AI binding failing closed (503), the "zero usable snapshots is a data failure even though nothing threw" data-unavailable guard, the data-only vs grounded narrative split, and the triple-quote prompt-fence defence (a lesson containing its own \"\"\" sequence must not forge a second fence boundary and break out to the model as a top-level instruction). Confirmed the session-gate test fails (503 instead of 401) when the auth check is removed, and the fence test fails when the defence() collapse is disabled; both reverted. 85/85 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/live-stats-api.test.mjs | 83/83 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep of HTML pages found no broken internal links/assets (the two "missing" images, assets/team/NAME.jpg and assets/results/YOUR_IMAGE.jpg, are the known owner-asset placeholders already tracked in Queued #1) and no copy typos. live-stats.js was the last functions/api/*.js file with zero test references — a public read that fans out to Discord's invite endpoint and Whop's REST API, each documented as degrading to null rather than breaking the page. Added tests/live-stats-api.test.mjs covering: the 60/min rate-limit guard tripping before either third-party call, Whop being skipped entirely (not fetched at all) when WHOP_API_KEY/WHOP_PRODUCT_ID are absent, a throwing Whop or Discord call degrading only that half to null, a full success shaping both halves correctly, and the 5-minute Cache-Control header. Confirmed the whop-skip assertion fails (returns fetched data instead of null) when the missing-key guard is removed, then reverted. 84/84 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/premium-market-analyst-api.test.mjs | 82/82 tests green going in (ticker.js coverage landed since the last log, closing out both probe-targeted endpoints). Live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. premium-market-analyst.js was the last payment-gated functions/api/*.js file with zero test references (session-cookie-only, no legacy Bearer path). Its own comment already documents a real fixed bug nothing had pinned: `lastClose > null` coerces to `lastClose > 0`, so aboveSma50/aboveSma200 must stay null rather than a fabricated boolean when there isn't yet 50/200 bars of history. Added tests/premium-market-analyst-api.test.mjs covering unauthenticated/tampered-cookie 401 before any upstream call, an out-of-set years param 400, missing Alpaca keys failing closed 503, under-60-bar history failing 502 instead of computing off a handful of days, the null-guard itself (90 bars: sma50 real, sma200 null, aboveSma200 must equal null not true), and the AI-binding-present vs absent narrative/dataOnly split. Confirmed the null-guard assertion fails (aboveSma200 comes back true) when the ternary guards are dropped to a bare `lastClose > metrics.smaN`, then reverted. 83/83 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/premium-stock-research-api.test.mjs | 80/80 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken (no dead links, no accessibility/copy regressions). premium-stock-research.js was the last payment-gated functions/api/*.js file with zero test references, and unlike the free stock-research.js it has two authorization paths -- the primary HttpOnly session cookie and a legacy HMAC Bearer token kept for research-engine's migration window -- neither of which had ever been pinned. Added tests/premium-stock-research-api.test.mjs covering: no-auth 401 before any upstream call, a tampered session cookie rejected, valid session cookie authorizing then failing closed (503) on missing Alpaca keys, the legacy Bearer path being skipped entirely when PREMIUM_ACCESS_CODES isn't configured, an expired legacy token rejected despite a correct signature, a forged-secret legacy token rejected, a valid legacy token authorizing exactly like the cookie path, the same Alpaca top-level-vs-wrapped snapshot shape fallback already fixed once elsewhere, and the marketCap fabrication guard staying null. Confirmed the expired-token assertion fails (502 instead of 401, request reaches the upstream call) when the legacy path's `payload.exp > Date.now()` check is replaced with `true`, then reverted. 81/81 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/content-api.test.mjs | 79/79 tests green going in; live-deployment curl probe again unavailable (this sandbox's network policy 403s all CONNECT attempts to pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. content.js is one of the two remaining probe-targeted endpoints (GET /api/content?type=schedule) with zero test references — ticker.js is the other and remains open for a future run. Its own comment documents a real fixed bug that nothing pinned: the trade_reviews ticker filter used to run in JS after SQL had already truncated to 60 rows by position, so a review for an older ticker could return count:0 even though it was in the table, just past the LIMIT; the fix pushes the match into the parameterized SQL (json_extract) so LIMIT applies to matches, not to all rows of the type. Added tests/content-api.test.mjs covering unknown-type 400, missing-RESEARCH_DB fail-closed 503, a malformed payload row being skipped rather than thrown, announcements sorting pinned-first, ORDERED_TYPES (team/faqs/results) sorting by their order field, the ticker filter being ignored outside trade_reviews, a D1 failure surfacing as 502, and the truncation-order bug scenario itself (70 rows, 6 ticker matches past position 60). Confirmed the truncation-order assertion fails (0 matches instead of 6) when the ticker match is reverted to a JS-only filter after an unfiltered LIMIT 60, then reverted. 80/80 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/yahoo-news-api.test.mjs | 78/78 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. yahoo-news.js was the second and last probe-targeted endpoint (GET /api/yahoo-news?symbol=AAPL) with zero test references, closing out that pair after stock-research.js. Added tests/yahoo-news-api.test.mjs covering: no symbol/topic 400s before any upstream call, an unrecognized ?topic= value 400s the same way rather than falling through as a caller-controlled query (the allowlist is closed), the closed-topic query mapping (?topic=forex must resolve to the fixed EURUSD=X query, matched case-insensitively), the mojibake repair, the https-only link sanitizer dropping a javascript: link instead of passing it through, dedup-by-link, and upstream failure surfacing the generic unavailable message. While building the mojibake case, found the function's own comment is misleading: it says a corrupted apostrophe arrives as "â€™", but the guard regex only matches 2-byte UTF-8-as-Latin-1 corruption (e.g. café -> cafÃ©) -- a 3-byte apostrophe corruption doesn't match the guard and passes through unrepaired. Confirmed empirically (not a functional bug since the repair still fires correctly on real 2-byte cases; left the comment alone since fixing it wasn't this run's scope, but the test's own comment documents the discrepancy so a future run doesn't re-litigate it). Verified both the mojibake-fix and https-only-link assertions fail when their respective guards are disabled, then reverted. 79/79 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/stock-research-api.test.mjs | 77/77 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. stock-research.js is one of the two probe-targeted endpoints (the run cycle's own step 2 curl check hits /api/stock-research?symbol=AAPL) with zero test references — yahoo-news.js is the other and remains open for a future run. Its own comment documents a real fixed bug that nothing pinned: Alpaca's /v2/stocks/snapshots response puts the symbol->snapshot map at the TOP LEVEL, not wrapped in a "snapshots" key (already wrong once in _lib/alpaca.js), so the handler accepts either shape defensively. Added tests/stock-research-api.test.mjs covering: missing-symbol 400, missing-Alpaca-keys 503 fail-closed before any upstream call, both the top-level and wrapped snapshot shapes returning the same quote, a symbol absent from the snapshot map failing as 502 rather than throwing or fabricating, changePercent math, and marketCap staying null (fabrication guard). Confirmed the shape-fallback assertion fails (502 instead of 200) when the `data.snapshots ?? data` fallback is narrowed back to `data.snapshots` only, then reverted. 78/78 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/logout-premium-api.test.mjs | 76/76 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. logout-premium.js was the last session-adjacent functions/api/*.js file with zero test references; its own comments promise the audit write is "best-effort" and the whole handler "never blocks logout" even if session verification throws, but neither promise was pinned. Added tests/logout-premium-api.test.mjs covering: cookie always cleared (byte-identical to buildClearCookie()) whether or not a session existed; no session means the audit write is never attempted at all; a valid session writes the documented audit row (event_type='logout', outcome='ok', subject_hash truncated to 16 chars); a D1 write failure and a garbage session cookie both still return ok:true with the cookie cleared. Confirmed the failure-mode test fails (uncaught error propagates) when both the inner best-effort catch and the outer never-block catch are removed, then reverted. 77/77 tests; commit pushed to origin/pj. | 2026-08-30 |
| claude (autonomous maintenance run) | pj: tests/content-sync-api.test.mjs | 75/75 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. content-sync.js — the hourly owner-content pipeline (Sheet -> HMAC bridge -> D1 -> Discord) — was the last functions/api/*.js file with zero test references. Its own comment already documented a real fixed bug that nothing pinned: capping the Discord-post CANDIDATE list at 10 (instead of capping the number actually posted) meant a genuinely new announcement past index 9 was silently dropped forever once the sheet held more than ten rows and the first ten were already-posted. Added tests/content-sync-api.test.mjs covering the cron-secret auth gate, config-missing 503s, bridge-unreachable/bad-shape 502s, malformed-row skip vs valid-row upsert, dry-run never calling Discord, and the exact 12-announcement/10-already-posted scenario the bug comment describes. Confirmed the key assertion fails (0 posted instead of 2) when the candidate-slicing bug is reintroduced, reverted. 76/76 tests; commit pushed to origin/pj. | 2026-08-29 |
| claude (autonomous maintenance run) | pj: tests/whop-webhook-api.test.mjs | 74/74 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, re-confirmed via agent-proxy status, not re-logged as a new finding). Full sweep found nothing else broken. whop-webhook.js — the payment-automation endpoint that mints/revokes access codes from real Whop purchases — was the last functions/api/*.js file with zero test references. Added tests/whop-webhook-api.test.mjs covering HMAC signature + timestamp-freshness verification, the atomic INSERT-OR-IGNORE idempotency claim (a replayed event must not mint a second code), and two invariants the handler's own comments already documented but nothing pinned: a revoke event with no member id must fail CLOSED (422, claim released) rather than being recorded as handled while access stays live, and an undeliverable grant (no Discord channel, or the delivery post itself fails) must delete the pending code row and release the claim so a retry can mint a fresh, deliverable code. Confirmed the fail-closed revoke assertion fails when that guard is disabled (200 instead of 422), reverted. 75/75 tests; commit pushed to origin/pj. | 2026-08-29 |
| claude (autonomous maintenance run) | pj: tests/verify-premium-api.test.mjs | 73/73 tests green going in; live-deployment curl probe again unavailable (same standing network-policy 403 on pj.vjm.pages.dev, not re-logged as a new finding). Full sweep found nothing else broken. verify-premium.js — the exact endpoint the probe's own step 2 curl check targets — was the last auth-critical functions/api/*.js file with zero test references, so added a unit test that pins the same "bad code + no turnstileToken → verification-failed" contract at the code level, plus soft-required-until-configured, fail-closed on a Turnstile network error, and the token never appearing in the JSON body. Confirmed the fail-closed assertion fails when turnstile.js's catch block is flipped to return true (fail open) instead of false; reverted. 74/74 tests; commit pushed to origin/pj. | 2026-08-29 |
| claude (autonomous maintenance run) | pj: tests/auth-google-api.test.mjs | 71/71 tests green going in; live-deployment curl probe again unavailable — same standing network-policy block on pj.vjm.pages.dev (re-confirmed via agent-proxy status, no new info, not re-logged as a separate finding). Full sweep found nothing broken. auth-google.js was now the only auth-adjacent functions/api/*.js file with zero test references; its own comment already documents a real fixed bug (Google tokeninfo returns email_verified as the STRING 'true'/'false', so a naive `=== false` check let unverified accounts through) but nothing pinned that fix. Added tests/auth-google-api.test.mjs: config-missing 503s, malformed-body 400s, the email_verified string regression, aud-mismatch rejection, no-Whop-match 404, and the yearly-plan session-expiry cap. Confirmed the key test fails by reverting the check to `=== false`, watched it fail (404 instead of 401), reverted. 72/72 tests; commit pushed to origin/pj. | 2026-08-29 |
| claude (autonomous maintenance run) | pj: tests/member-status-api.test.mjs | 70/70 tests were green going in; live-deployment curl probe (step 2 of the run cycle) could not execute — this sandbox's network policy 403s all CONNECT attempts to pj.vjm.pages.dev, confirmed via the agent-proxy status endpoint, so that step is unverifiable from this environment rather than evidence of a site problem. No broken pages, dead links, heading-order/label/contrast gaps, or copy errors found on a full sweep (prior runs already closed those). Picked test coverage: check-member-status.js was the only functions/api/*.js file with zero test references, and its own comment promises callers can't tell an inactive handle from an unknown one — added tests/member-status-api.test.mjs covering 400/503/200/404 paths and asserting a known-inactive vs never-seen handle return byte-identical messages; confirmed the test fails if that promise is broken (temporarily patched the source, watched it fail, reverted). 71/71 tests; commit 33fb62e pushed to origin/pj. | 2026-08-29 |
| Codex /root | pj: index.html; tests/pj-futures.test.mjs; assets/pj-intro-poster.jpg | Replaced the generic thumbnail with PJ's own opening video frame, simplified the futures tape, and extended the red/black lightning gradient across sections; 56/56 tests pass. | 2026-08-27 |
| claude-code | pj: index.html - fixed opencode's local UI batch before commit (nav-highlight, premium dropdown, quiz, review-bars, growth/tick/sizing sims, hero video) | Found and fixed real breakage before pushing: #results left unclosed (results-grid deleted, nested #schedule inside it), Futures Tick Calc's <select id="fc-contract"> deleted leaving orphaned <option>s + unmatched closing div, and 4 new sim buttons (runGrowthSim/runPropPayout/calcTickSim/calcSizingSim) had zero matching JS - implemented all four (bidirectional NQ/MNQ/ES/MES point math for tick/sizing, daily-target x days/week/month for prop payout), fixed setSimMode's stale investing->prop ids, removed dead calcOptions/calcRR, removed a duplicate eager <video autoplay> in the hero (same file as the existing tested lazy facade), fixed review-bar fill (span width:% is ignored without display:block - real pre-existing bug). Added assets/pj-intro.mp4 (compressed, 16.1MB). 58/58 tests, verified all 4 calc functions + fill fix live via local server; commit 4391d4b, pushed to origin/pj. | 2026-08-25 |
| claude-code | pj: Phase 3 CMS expansion (schedule/team/faqs/bundles/stats/results) + _headers | integrations-core.js sanitizers + CONTENT_TYPES, content.js TYPES set, Code.gs reads 6 new sheet tabs, index.html client renderers for all 6 types (fallback markup kept, only replaced when CMS has rows; schedule renderer also refreshes PJ_SESSIONS times), _headers appended /pj noindex + immutable asset caching; 56/56 tests (10 new), verified against a local static server with /api/content 404ing - all fallbacks render correctly, no console errors; commits c2e8550 + 4f68139, pushed to origin/pj. Did NOT touch Tasks 1/2/5 (Cloudflare/GitHub secrets, wrangler, Lighthouse) - need owner credentials. | 2026-08-25 |
| ox-alpha | pj: index FX + team blanks + results wall + premium-market-analyst.js + ai.js guardrails + guidance panel | Ambient FX within perf budget (Perf 67/CLS 0 after trimming hero text-shimmer), PJ + 5 blank team slots, blank owner-ready results wall, premium Alpaca AI analyst (1/3/5Y QQQ, gated, data-grounded), chatbot now directs visitors; 46/46 tests, browser checks clean; commit c2f07f8; /pj synced (main 0e5dbd9) | 2026-08-24 |
| ox-alpha | pj branch: index/premarket + tests (Phase 1+2 in one pass) | Futures-first redesign: $100 Futures + $129 All-Markets bundles, Whop showcase (features/reviews/49,136), video facade above bundles, team of 5, week schedule grid + session countdown, FAQ + JSON-LD, socials, futures ticker; 44/44 tests, 14/14 browser checks, Perf 69 CLS 0; commit 6afa0c5; /pj preview synced on main ccf1516 | 2026-08-24 |
| ox-alpha | pj branch: index/premium-guidance/prop-firms + assets | Visual polish pass, scroll-reveal, chatbot wiring, lesson companion, prop-firms accuracy comments; tests 34/34; commit 6fb2dfc | 2026-08-24 |
| ox-alpha | main branch: /pj folder | Refreshed static preview from pj HEAD, noindex hygiene; commit 528cf58 | 2026-08-24 |

## Requests (cross-instance needs)

| From | Need | For |
|------|------|-----|
| — | | |

## Blocked on owner

| Item | What's needed |
|------|---------------|
| Pre-market brief auto-post | GitHub secrets RESEARCH_REFRESH_URL + RESEARCH_CRON_SECRET |
| Announcements → Discord auto-post | Cloudflare vars DISCORD_ANNOUNCEMENTS_WEBHOOK + CONTENT_DISCORD_DRYRUN=false |
| Whop purchase codes | WHOP_WEBHOOK_SECRET + D1 binding + DISCORD_WHOP_CODES_WEBHOOK |
| Two-tier entitlement split (commit 2f82e36) | WHOP_PRODUCTS_FUTURES + WHOP_PRODUCTS_COMPLETE in both Production and Preview — until set, resolveTier() grants everyone `complete` (`unconfigured_default`), so the $100 and $129 products stay identical; see docs/ENTITLEMENTS.md |
