{
  "name": "vjm-site",
  "private": true,
  "type": "module",
  "scripts": {
    "check:syntax": "node tools/check-syntax.mjs",
    "test": "npm run check:syntax && node --test \"tests/*.test.mjs\""
  }
}
